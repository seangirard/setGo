A simple plugin pattern for your jQuery application.

ToDo:
-Finish README

Known Issues:
- Unecessary recursion in the object constructor ($obj)?
- Method calls are wrapped in try/catch; you unwrap them
